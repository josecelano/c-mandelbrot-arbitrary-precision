#!/usr/bin/env bash

# It generates all the files in samples folder, but in output folder.
# They have to be manually added to the repo.

# [OUTPUT_FORMAT] [CENTER_X] [CENTER_Y] [WIDTH] [HEIGHT] [RES_X] [RES_Y] [COLOR_MAP|ASCII_MAP] [FILENAME]

# TODO. WIP. Only verbose options are added as console options

./mandelbrot -p